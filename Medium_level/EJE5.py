#Define una función que tome una cadena y cuente el número de vocales en la cadena.
def contar_vocales(cadena):
  return sum(1 for letra in cadena if letra.lower() in 'aeiou')
  #suma 1 para variables dentro de otra variable si alguna de esas variables es (a e i o u)
  #.lower() = lo convierte todo en minúsculas.
print(contar_vocales('experimento, aaa, eee, iii'))

def cnteo(x, y)
for x in y all.scoppe [distinct]{37 for "disy[]}